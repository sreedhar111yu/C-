#include<bits/stdc++.h>
using namespace std;
int main(){
    int noOfRow=5;
    for(int row=1;row<=noOfRow;row++){
        for(int col=1;col<=row;col++){
            cout<<("* ");
        }
        cout<<("\n");
    }
}